# FastAPI 版本的 LDDC 依赖
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# LDDC 核心依赖
httpx[brotli,http2]
mutagen
diskcache
charset-normalizer
pyaes
psutil
pyobjc-framework-Cocoa; sys_platform == 'darwin'

# 可选：如果需要GUI功能，取消注释下面这行
# PySide6-Essentials>=6.8.0